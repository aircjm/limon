pub fn vec_loop(mut v: Vec<i32>) -> Vec<i32> {
    for i in /* What should we iterate over here? */ {
        // Fill this up so that each element in the Vec `v` is multiplied by 2.
    }
    // At this point, `v` should be equal to [4, 8, 12, 16, 20].
    v
}